import 'package:flutter/material.dart';

class CoinTossPage extends StatelessWidget {
  const CoinTossPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Coin Toss'), centerTitle: true),
    );
  }
}
